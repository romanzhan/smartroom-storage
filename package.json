{
  "name": "storage-calc",
  "version": "1.0.0",
  "description": "SmartRoom storage calculator (Vite + vanilla JS)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:wp": "vite build --base=./",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^8.0.0",
    "gh-pages": "^6.3.0",
    "handlebars": "^4.7.8",
    "postcss": "^8.5.8",
    "vite": "^8.0.3",
    "vitest": "^3.2.4"
  }
}
